{#
    Template réutilisable pour la gestion Client/Prospect en mode édition v2
    Utilise le service JavaScript ClientProspectService unifié
    
    Variables attendues :
    - form : Le formulaire Symfony
    - devis : L'entité Devis en cours d'édition
    - show_header : Afficher l'en-tête de section (défaut: true)
#}

{% set show_header = show_header ?? true %}

<!-- Section unifiée Client, Commercial, Adresses et contacts -->
<div class="form-section" id="contact-selection">
    <div class="form-section-header">
        <h5 class="mb-0"><i class="fas fa-address-card me-2"></i>Client et contacts</h5>
    </div>
    <div class="form-section-body">
        <!-- Client / Prospect et Commercial en premier -->
        <div class="row mb-3">
            <div class="col-md-6">
                <label for="devis_client" class="form-label">Client / Prospect</label>
                <div class="d-flex align-items-center gap-2">
                    {{ form_widget(form.client, {
                        'attr': {'class': 'form-control form-select client-select', 'style': 'flex-grow: 1;', 'tabindex': '5'}
                    }) }}
                    
                    <!-- Boutons d'action client -->
                    <button type="button" class="btn btn-success btn-sm" id="add-client-btn" 
                            title="Ajouter un nouveau client">
                        <i class="fas fa-plus"></i>
                    </button>
                    <button type="button" class="btn btn-primary btn-sm" id="edit-client-btn" 
                            title="Modifier le client sélectionné" 
                            style="{{ devis.client ? '' : 'display: none;' }}">
                        <i class="fas fa-edit"></i>
                    </button>
                </div>
                {{ form_errors(form.client) }}
            </div>
            <div class="col-md-6">
                {{ form_row(form.commercial, {
                    'label': 'Commercial',
                    'label_attr': {'class': 'form-label'},
                    'attr': {'class': 'form-select'}
                }) }}
            </div>
        </div>
        
        <!-- Contacts et adresses -->
        <div class="row">
            <!-- Contact et adresse de livraison -->
            <div class="col-md-6">
                <div class="mb-3">
                    <label class="form-label">Contact en charge du projet</label>
                    <div class="d-flex align-items-center gap-2">
                        {{ form_widget(form.contactLivraison, {
                            'attr': {'class': 'form-select contact-select', 'id': 'contact_defaut', 'style': 'flex-grow: 1;'}
                        }) }}
                        
                        <!-- Boutons d'action contact livraison -->
                        <button type="button" class="btn btn-success btn-sm" id="add-contact-btn" 
                                title="Ajouter un nouveau contact">
                            <i class="fas fa-plus"></i>
                        </button>
                        <button type="button" class="btn btn-primary btn-sm" id="edit-contact-livraison-btn" 
                                title="Modifier le contact sélectionné" 
                                style="{{ devis.contactLivraison ? '' : 'display: none;' }}">
                            <i class="fas fa-edit"></i>
                        </button>
                    </div>
                    {{ form_errors(form.contactLivraison) }}
                </div>
                
                <!-- Menu déroulant adresse de livraison -->
                <div class="mb-3">
                    <label class="form-label">Adresse de livraison</label>
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <select class="form-select" id="adresse_livraison_select" name="adresse_livraison_select" style="flex-grow: 1;">
                            <option value="">Choisir une adresse...</option>
                        </select>
                        
                        <!-- Boutons d'action adresse livraison -->
                        <button type="button" class="btn btn-success btn-sm" id="add-address-livraison-btn" 
                                title="Ajouter une nouvelle adresse">
                            <i class="fas fa-plus"></i>
                        </button>
                        <button type="button" class="btn btn-primary btn-sm" id="edit-address-livraison-btn" 
                                title="Modifier l'adresse sélectionnée" style="display: none;">
                            <i class="fas fa-edit"></i>
                        </button>
                    </div>
                    
                    <!-- Champs d'adresse modifiables pour ce devis (masqués) -->
                    <div class="border rounded p-3 bg-light" style="display: none;">
                        <small class="text-muted mb-2 d-block">Adresse pour ce devis (modifiable)</small>
                        <div class="row">
                            <div class="col-md-12">
                                {{ form_row(form.tiersAdresseLivraison, {
                                    'label': 'Adresse',
                                    'attr': {'class': 'form-control'}
                                }) }}
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                {{ form_row(form.tiersCodePostalLivraison, {
                                    'label': 'Code postal',
                                    'attr': {
                                        'class': 'form-control postal-autocomplete', 
                                        'maxlength': '5',
                                        'data-target-ville': 'devis_tiersVilleLivraison'
                                    }
                                }) }}
                            </div>
                            <div class="col-md-8">
                                {{ form_row(form.tiersVilleLivraison, {
                                    'label': 'Ville',
                                    'attr': {
                                        'class': 'form-control ville-autocomplete',
                                        'data-target-postal': 'devis_tiersCodePostalLivraison'
                                    }
                                }) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Contact et adresse de facturation -->
            <div class="col-md-6">
                <div class="mb-3">
                    <label class="form-label">Contact de facturation</label>
                    <div class="d-flex align-items-center gap-2">
                        {{ form_widget(form.contactFacturation, {
                            'attr': {'class': 'form-select contact-select', 'id': 'contact_facturation', 'style': 'flex-grow: 1;'}
                        }) }}
                        
                        <!-- Boutons d'action contact facturation -->
                        <button type="button" class="btn btn-success btn-sm" id="add-contact-facturation-btn" 
                                title="Ajouter un nouveau contact">
                            <i class="fas fa-plus"></i>
                        </button>
                        <button type="button" class="btn btn-primary btn-sm" id="edit-contact-facturation-btn" 
                                title="Modifier le contact sélectionné"
                                style="{{ devis.contactFacturation ? '' : 'display: none;' }}">
                            <i class="fas fa-edit"></i>
                        </button>
                    </div>
                    {{ form_errors(form.contactFacturation) }}
                </div>
                
                <!-- Menu déroulant adresse de facturation -->
                <div class="mb-3">
                    <label class="form-label">Adresse de facturation</label>
                    <div class="d-flex align-items-center gap-2 mb-2">
                        <select class="form-select" id="adresse_facturation_select" name="adresse_facturation_select" style="flex-grow: 1;">
                            <option value="">Choisir une adresse...</option>
                        </select>
                        
                        <!-- Boutons d'action adresse facturation -->
                        <button type="button" class="btn btn-success btn-sm" id="add-address-facturation-btn" 
                                title="Ajouter une nouvelle adresse">
                            <i class="fas fa-plus"></i>
                        </button>
                        <button type="button" class="btn btn-primary btn-sm" id="edit-address-facturation-btn" 
                                title="Modifier l'adresse sélectionnée" style="display: none;">
                            <i class="fas fa-edit"></i>
                        </button>
                    </div>
                    
                    <!-- Champs d'adresse modifiables pour ce devis (masqués) -->
                    <div class="border rounded p-3 bg-light" style="display: none;">
                        <small class="text-muted mb-2 d-block">Adresse pour ce devis (modifiable)</small>
                        <div class="row">
                            <div class="col-md-12">
                                {{ form_row(form.tiersAdresse, {
                                    'label': 'Adresse',
                                    'attr': {'class': 'form-control'}
                                }) }}
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                {{ form_row(form.tiersCodePostal, {
                                    'label': 'Code postal',
                                    'attr': {
                                        'class': 'form-control postal-autocomplete', 
                                        'maxlength': '5',
                                        'data-target-ville': 'devis_tiersVille'
                                    }
                                }) }}
                            </div>
                            <div class="col-md-8">
                                {{ form_row(form.tiersVille, {
                                    'label': 'Ville',
                                    'attr': {
                                        'class': 'form-control ville-autocomplete',
                                        'data-target-postal': 'devis_tiersCodePostal'
                                    }
                                }) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Email d'envoi automatique et Modèle de document -->
        <div class="row">
            <div class="col-md-6">
                {{ form_row(form.emailEnvoiAutomatique, {
                    'label': 'Email d\'envoi automatique',
                    'label_attr': {'class': 'form-label'},
                    'attr': {'class': 'form-control', 'placeholder': 'email@example.com'}
                }) }}
                <small class="text-muted">
                    <i class="fas fa-paper-plane me-1"></i>Ce devis sera envoyé automatiquement à cette adresse
                </small>
            </div>
            <div class="col-md-6">
                {{ form_row(form.modeleDocument, {
                    'label': 'Modèle de document',
                    'label_attr': {'class': 'form-label'},
                    'attr': {'class': 'form-select'}
                }) }}
                <small class="text-muted">
                    <i class="fas fa-info-circle me-1"></i>Le modèle sélectionné sera utilisé lors de la génération PDF du devis.
                </small>
            </div>
        </div>
    </div>
</div>

<!-- Les services JavaScript sont inclus dans le template principal -->

<script>
// Initialisation du service ClientProspectService pour l'édition
(function() {
    'use strict';
    
    let clientService = null;
    
    function initClientProspectServiceEdit() {
        // Configuration du service pour le mode édition
        const config = {
            debug: true,
            mode: 'edit',
            disableModalButtons: true, // IMPORTANT: Ne pas gérer les boutons de modales
            selectors: {
                clientSelect: '#devis_client',  // ID Symfony correct pour le champ client
                contactLivraisonSelect: '#devis_contactLivraison',  // Vrais IDs Symfony 
                contactFacturationSelect: '#devis_contactFacturation', // Vrais IDs Symfony
                adresseLivraisonSelect: '#adresse_livraison_select',
                adresseFacturationSelect: '#adresse_facturation_select',
                contactSelection: '#contact-selection',
                editClientBtn: '#edit-client-btn',
                editContactLivraisonBtn: '#edit-contact-livraison-btn',
                editContactFacturationBtn: '#edit-contact-facturation-btn',
                editAddressLivraisonBtn: '#edit-address-livraison-btn',
                editAddressFacturationBtn: '#edit-address-facturation-btn'
            }
        };
        
        // Créer et initialiser le service
        console.log('🔍 [CLIENT_EDIT_SELECTOR] Création de ClientProspectService avec config:', config);
        clientService = new ClientProspectService(config);
        
        // Rendre le service accessible globalement pour les autres services
        window.clientProspectService = clientService;
        
        console.log('🔍 [CLIENT_EDIT_SELECTOR] Appel de clientService.init()...');
        if (clientService.init()) {
            console.log('✅ [CLIENT_EDIT_SELECTOR] ClientProspectService initialisé pour l\'édition');
            
            // PATCH TEMPORAIRE: Désactiver les boutons de ClientProspectService
            // car il ne respecte pas le flag disableModalButtons
            console.log('🛑 [PATCH] Suppression des event listeners ajoutés par ClientProspectService...');
            const addClientBtn = document.getElementById('add-client-btn');
            if (addClientBtn) {
                const newBtn = addClientBtn.cloneNode(true);
                addClientBtn.parentNode.replaceChild(newBtn, addClientBtn);
                console.log('🛑 [PATCH] Event listeners du bouton add-client-btn supprimés');
            }
            
            // Initialiser avec le client sélectionné si il y en a un
            const clientSelect = document.querySelector('#devis_client');
            if (clientSelect?.value) {
                console.log('🔄 Initialisation avec client:', clientSelect.value);
                clientService.handleClientChange(clientSelect.value);
            }
        } else {
            console.error('❌ Échec de l\'initialisation du ClientProspectService');
        }
    }
    
    function setupAddressToFormSync() {
        const adresseLivraisonSelect = document.getElementById('adresse_livraison_select');
        const adresseFacturationSelect = document.getElementById('adresse_facturation_select');
        
        adresseLivraisonSelect?.addEventListener('change', function() {
            const addressId = this.value;
            if (addressId && clientService?.currentAddresses) {
                const address = clientService.currentAddresses.find(addr => addr.id == addressId);
                if (address) {
                    populateAddressFields('livraison', address);
                }
            }
        });
        
        adresseFacturationSelect?.addEventListener('change', function() {
            const addressId = this.value;
            if (addressId && clientService?.currentAddresses) {
                const address = clientService.currentAddresses.find(addr => addr.id == addressId);
                if (address) {
                    populateAddressFields('facturation', address);
                }
            }
        });
    }
    
    function populateAddressFields(type, address) {
        const prefix = type === 'livraison' ? 'tiersAdresseLivraison' : 'tiersAdresse';
        const postalPrefix = type === 'livraison' ? 'tiersCodePostalLivraison' : 'tiersCodePostal';
        const villePrefix = type === 'livraison' ? 'tiersVilleLivraison' : 'tiersVille';
        
        // Remplir les champs avec les données de l'adresse
        const addressField = document.getElementById(`devis_${prefix}`);
        const postalField = document.getElementById(`devis_${postalPrefix}`);
        const villeField = document.getElementById(`devis_${villePrefix}`);
        
        if (addressField) addressField.value = address.ligne1 || '';
        if (postalField) postalField.value = address.codePostal || '';
        if (villeField) villeField.value = address.ville || '';
        
        console.log(`📍 Champs ${type} remplis avec l'adresse:`, address.label);
    }
    
    // Initialiser quand le DOM est prêt
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initClientProspectServiceEdit);
    } else {
        initClientProspectServiceEdit();
    }
    
    // Nettoyer avant déchargement de la page
    window.addEventListener('beforeunload', function() {
        if (clientService) {
            clientService.destroy();
        }
    });
    
    // Initialiser Select2 pour l'autocomplétion du champ client
    setTimeout(function() {
        if (window.$ && typeof $.fn.select2 !== 'undefined') {
            const clientSelect = $('#devis_client');
            if (clientSelect.length && !clientSelect.hasClass('select2-hidden-accessible')) {
                clientSelect.select2({
                    placeholder: 'Choisir un client/prospect...',
                    allowClear: true,
                    language: 'fr',
                    width: '100%',
                    dropdownAutoWidth: true,
                    theme: 'bootstrap-5',
                    selectOnClose: false,
                    escapeMarkup: function (markup) { return markup; }
                });
                
                // Ajuster la hauteur pour correspondre aux autres champs
                $('.select2-container--bootstrap-5 .select2-selection--single').css({
                    'height': '38px',
                    'padding': '0.375rem 0.75rem',
                    'line-height': '1.5'
                });
                
                // Améliorer la navigation clavier
                const select2Container = clientSelect.next('.select2-container');
                
                // Gérer le focus sur le container Select2
                select2Container.on('focus', function() {
                    clientSelect.select2('open');
                });
                
                // Gérer la navigation Tab depuis le champ statut
                $('#devis_statut').on('keydown', function(e) {
                    if (e.key === 'Tab' && !e.shiftKey) {
                        e.preventDefault();
                        // Donner le focus au champ Select2 et l'ouvrir
                        setTimeout(function() {
                            clientSelect.select2('open');
                        }, 50);
                    }
                });
                
                // Permettre de taper directement quand le Select2 reçoit le focus
                clientSelect.on('select2:open', function() {
                    const searchField = $('.select2-search__field');
                    if (searchField.length) {
                        searchField.focus();
                    }
                });
                
                // Gérer la navigation Tab depuis le Select2
                clientSelect.on('select2:close', function(e) {
                    // Si on ferme avec Tab, aller au champ suivant
                    setTimeout(function() {
                        $('#devis_commercial').focus();
                    }, 50);
                });
                
                console.log('✅ Select2 initialisé pour le champ client avec navigation clavier améliorée');
            }
        }
    }, 1000);
    
    // ==========================================
    // PAS D'INITIALISATION DE MODALES ICI
    // ==========================================
    // Les services de modales sont initialisés dans edit.html.twig
    // pour éviter toute duplication
    
})();
</script>